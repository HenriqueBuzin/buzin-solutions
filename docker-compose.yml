version: '3.9'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ENV=development
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
