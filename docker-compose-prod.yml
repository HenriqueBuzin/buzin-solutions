services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile-prod
    environment:
      - ENV=production
    ports:
      - "127.0.0.1:3000:80"
    restart: unless-stopped
    container_name: frontend
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
